const settings = {
  width: 1920,
  heigh: 1080,
  contentWidth: 1920,
  contentHeight: 1080,
  procentWidth: 75,
  procentHeight: 80,
  eventStart: 'mousedown touchstart',
  eventFinish:'mouseup touchend',
  eventMove: 'mousemove touchmove',
  eventLeave: 'mouseleave touchcancel'
}

export default settings;